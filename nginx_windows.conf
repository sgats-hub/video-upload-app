worker_processes  1;

events {
    worker_connections  1024;
}

http {
    include       mime.types;
    default_type  application/octet-stream;

    sendfile        on;
    keepalive_timeout  65;

    gzip  on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml;

    client_max_body_size 5G;

    upstream backend {
        server 127.0.0.1:5000;
    }

    # HTTP服务器（主服务器）
    server {
        listen       80;
        listen       8088;
        server_name  localhost 127.0.0.1 2minutevideos.com www.2minutevideos.com;

        # 前端静态文件 - index.html 不缓存
        location = /index.html {
            root   E:/video-upload-app/dist;
            add_header Cache-Control "no-store, no-cache, must-revalidate, proxy-revalidate";
            add_header Pragma "no-cache";
            add_header Expires "Thu, 01 Jan 1970 00:00:00 GMT";
        }

        # 静态资源缓存（带hash的文件可以长期缓存）
        location ~* \.(js|css|png|jpg|jpeg|gif|svg|woff|woff2|ttf|eot)$ {
            root   E:/video-upload-app/dist;
            add_header Cache-Control "public, max-age=31536000, immutable";
        }

        # 其他路径
        location / {
            root   E:/video-upload-app/dist;
            try_files $uri $uri/ /index.html;
            index  index.html index.htm;
        }

        # API请求转发到Flask
        location /api/ {
            proxy_pass http://backend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_connect_timeout 300;
            proxy_send_timeout 300;
            proxy_read_timeout 300;
        }

        error_page   500 502 503 504  /50x.html;
        location = /50x.html {
            root   html;
        }
    }

    # HTTPS服务器（获取证书后取消注释启用）
    # server {
    #     listen       443 ssl;
    #     http2 on;
    #     server_name  2minutevideos.com www.2minutevideos.com;

    #     ssl_certificate      C:/Certbot/live/2minutevideos.com/fullchain.pem;
    #     ssl_certificate_key  C:/Certbot/live/2minutevideos.com/privkey.pem;

    #     ssl_session_cache    shared:SSL:1m;
    #     ssl_session_timeout  5m;
    #     ssl_ciphers          HIGH:!aNULL:!MD5;
    #     ssl_prefer_server_ciphers  on;

    #     location / {
    #         root   E:/video-upload-app/dist;
    #         try_files $uri $uri/ /index.html;
    #         index  index.html index.htm;
    #     }

    #     location /api/ {
    #         proxy_pass http://backend;
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #         proxy_set_header X-Forwarded-Proto $scheme;
    #         proxy_connect_timeout 300;
    #         proxy_send_timeout 300;
    #         proxy_read_timeout 300;
    #     }
    # }

    # HTTP重定向到HTTPS（获取证书后取消注释启用）
    # server {
    #     listen       80;
    #     server_name  2minutevideos.com www.2minutevideos.com;
    #     return 301 https://$host$request_uri;
    # }
}